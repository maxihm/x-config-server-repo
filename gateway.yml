server:
  port: 8080
 
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8083/eureka

spring:
  cloud:
    gateway:
      routes:
      - id: companyModule
        uri: lb://company
        predicates:
        - Path=/company/**